import { FontClickDirective } from './font-click.directive';

describe('FontClickDirective', () => {
  it('should create an instance', () => {
    const directive = new FontClickDirective();
    expect(directive).toBeTruthy();
  });
});
